{
  "name": "surda-catalog-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Fastify + SQLite backend foundation for the Sarda catalog (Blink replacement). Not connected to the frontend yet.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "^5.2.1",
    "fastify-plugin": "^5.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
